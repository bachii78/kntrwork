#ifndef TASK_HPP
#define TASK_HPP
#include <iostream>
#include <time.h>

void randomFill(int*, int, int, int);

void show(int*, int);

void randomFill1(int*, int, int, int);

int showEl1(const int*, int, int, int);

int showEl(const int*, int, int, int);

void swap(int&, int&);

void selectionSort(int*, int);

int search(int*, int, int);

void random(int*, int);

void print(int*, int);

int getMax(const int*, int);

int getMin(const int*, int);

#endif //TASK_HPP
